# Docker Image for Cross-Compiling Rust appplications

## Supported Targets

The image supports the following targets:

* `x86_64-unknown-linux-gnu`
* `aarch64-unknown-linux-gnu`
* `x86_64-pc-windows-gnu` 